# Vercel > Project > Settings > Environment Variables

# Your Render API URL. Leave blank in local dev — Vite proxies /v1 to :8000.
VITE_API_BASE_URL=

# Same client ID as the backend's GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
